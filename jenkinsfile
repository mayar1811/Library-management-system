pipeline {
    agent any
    environment {
        DOCKER_CREDENTIALS_ID = 'mayar-docker-hub-credentials'
        DOCKER_IMAGE = 'library_web'
        DOCKER_TAG = 'latest'
        DOCKER_HUB_REPO = 'mayaremam/library_management_system_V1' // Docker Hub username/repo
    }
    stages {
        stage('Build') {
            steps {
                script {
                    // Build Docker image with the correct tag
                    docker.build("${DOCKER_HUB_REPO}:${DOCKER_TAG}")
                }
            }
        }
        stage('Push') {
            steps {
                script {
                    // Log in to Docker Hub and push the image
                    docker.withRegistry('https://index.docker.io/v1/', "${DOCKER_CREDENTIALS_ID}") {
                        // Push Docker image to Docker Hub
                        docker.image("${DOCKER_HUB_REPO}:${DOCKER_TAG}").push()
                    }
                }
            }
        }
    }
    post {
        always {
            // Clean up resources or notify stakeholders
            echo 'Cleaning up...'
        }
    }
}

 


